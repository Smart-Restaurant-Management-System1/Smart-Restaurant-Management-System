# Restaurant Web Frontend

React + Vite frontend.

After creating the actual React source files:
1. Open this directory.
2. Run `npm install`.
3. Run `npm run dev`.

API base URLs can be configured through environment variables.
